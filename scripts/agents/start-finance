#!/usr/bin/env bash
# Launch the finance agent (SOL family). On-demand only — see .agents/roles/finance.md.
set -euo pipefail
source "$(dirname "${BASH_SOURCE[0]}")/_common.sh"
launch "${SOL_MODEL:-}" ".agents/roles/finance.md"
