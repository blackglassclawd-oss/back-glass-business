#!/usr/bin/env bash
# Launch the bizdev agent (SOL family). On-demand only — see .agents/roles/bizdev.md.
set -euo pipefail
source "$(dirname "${BASH_SOURCE[0]}")/_common.sh"
launch "${SOL_MODEL:-}" ".agents/roles/bizdev.md"
